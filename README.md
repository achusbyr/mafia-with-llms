# mafia-with-llms
(Very WIP) Uses OpenRouter. You can set your own API key in `API_KEY.txt` or modify `ai_interface.rs` to use another API.

Uses tools, won't work if the API or model doesn't support them!
